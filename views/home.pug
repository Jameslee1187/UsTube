extends layouts/main
include mixins/videoBlock

block content
    .home-videos
        each item in videos 
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })

                //-the plus sign iterates and adds each item underneath from videoBLock mixin for each video